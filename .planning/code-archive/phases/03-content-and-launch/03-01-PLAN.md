---
phase: 03-content-and-launch
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/BaseLayout.astro
  - src/pages/methodology.astro
  - src/pages/index.astro
  - src/styles/global.css
  - public/og.png
autonomous: true
requirements: [CONT-01, CONT-02, CONT-03, CONT-04, SITE-03]

must_haves:
  truths:
    - "Sharing the site URL on social platforms shows a populated Open Graph card with site name, description, and preview image"
    - "A methodology page exists at /methodology with four expandable sections that start collapsed"
    - "Dashboard shows a visible disclaimer explaining what the score is and is not"
    - "An About section on the dashboard explains the project purpose and approach"
    - "Navigation links connect the dashboard and methodology pages"
  artifacts:
    - path: "src/layouts/BaseLayout.astro"
      provides: "OG meta tags, Twitter card meta, nav bar, description prop"
      contains: "og:image"
    - path: "src/pages/methodology.astro"
      provides: "Methodology page with four expandable detail sections"
      contains: "<details>"
    - path: "src/pages/index.astro"
      provides: "Disclaimer section and About section on dashboard"
      contains: "disclaimer"
    - path: "public/og.png"
      provides: "Static 1200x630 Open Graph preview image"
    - path: "src/styles/global.css"
      provides: "Styles for nav, methodology page, disclaimer, about section"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "public/og.png"
      via: "new URL(ogImage, Astro.site)"
      pattern: "og:image.*content.*ogImageURL"
    - from: "src/layouts/BaseLayout.astro"
      to: "src/pages/methodology.astro"
      via: "nav link href /methodology"
      pattern: 'href.*methodology'
    - from: "src/pages/methodology.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "import and wrapping content in BaseLayout"
      pattern: "import BaseLayout"
---

<objective>
Add supporting content (methodology page, disclaimer, about section), site-wide navigation, and Open Graph meta tags for social sharing.

Purpose: Establish credibility, responsible framing, and social shareability before public launch. The methodology page, disclaimer, and about section give visitors context for what the score means and does not mean. OG meta tags ensure shared links display properly on social platforms.

Output: Methodology page at /methodology, disclaimer and about sections on dashboard, OG meta in all pages, site-wide nav bar, static OG image.
</objective>

<execution_context>
@C:/Users/matts/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matts/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-and-launch/03-RESEARCH.md
@.planning/phases/02-dashboard/02-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/layouts/BaseLayout.astro (current):
```astro
---
import '../styles/global.css';

export interface Props {
  title: string;
}
const { title } = Astro.props;
---
```

From astro.config.mjs:
```js
export default defineConfig({
  output: 'static',
  site: 'https://revolutionindex.pages.dev',
});
```

From src/styles/global.css (custom properties):
```css
:root {
  --zone-stable: #40916c;
  --zone-elevated: #f4a261;
  --zone-crisis: #e76f51;
  --zone-revolution: #c1121f;
  --bg-primary: #0f0f0f;
  --bg-card: #1a1a1a;
  --text-primary: #f5f5f5;
  --text-secondary: #a0a0a0;
  --accent: #e85d04;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend BaseLayout with OG meta tags, site-wide nav, and create static OG image</name>
  <files>src/layouts/BaseLayout.astro, src/styles/global.css, public/og.png</files>
  <action>
**1. Update `src/layouts/BaseLayout.astro`:**

Extend the Props interface to accept `description` and `ogImage` optional props with sensible defaults:

```astro
---
import '../styles/global.css';

export interface Props {
  title: string;
  description?: string;
  ogImage?: string;
}

const {
  title,
  description = 'A data-driven dashboard tracking the probability of revolutionary political change in the United States.',
  ogImage = '/og.png',
} = Astro.props;

const canonicalURL = new URL(Astro.url.pathname, Astro.site ?? 'http://localhost:4321');
const ogImageURL = new URL(ogImage, Astro.site ?? 'http://localhost:4321');
---
```

Add OG and Twitter meta tags in `<head>` after the existing `<meta name="description">` tag. Replace the existing hardcoded description meta with the prop-driven one:

```html
<meta name="description" content={description} />

<!-- Open Graph -->
<meta property="og:type" content="website" />
<meta property="og:url" content={canonicalURL} />
<meta property="og:site_name" content="Revolution Index" />
<meta property="og:title" content={title} />
<meta property="og:description" content={description} />
<meta property="og:image" content={ogImageURL} />
<meta property="og:image:width" content="1200" />
<meta property="og:image:height" content="630" />

<!-- Twitter/X Card -->
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:title" content={title} />
<meta name="twitter:description" content={description} />
<meta name="twitter:image" content={ogImageURL} />
```

Add a minimal `<nav>` element inside `<body>`, BEFORE `<slot />`:

```html
<nav class="site-nav" aria-label="Main navigation">
  <a href="/" class="nav-logo">Revolution Index</a>
  <div class="nav-links">
    <a href="/">Dashboard</a>
    <a href="/methodology">Methodology</a>
  </div>
</nav>
<slot />
```

**2. Add nav styles to `src/styles/global.css`:**

Add a new section after the Base Reset section for the nav bar. The nav should be:
- Sticky at top with `position: sticky; top: 0; z-index: 100`
- Dark background matching `--bg-primary` with slight transparency: `background: rgba(15, 15, 15, 0.95); backdrop-filter: blur(8px)`
- Flexbox row with space-between alignment
- `.nav-logo`: bold, `--text-primary` color, no underline, font-size 1.1rem
- `.nav-links a`: `--text-secondary` color, no underline, font-size 0.9rem, hover transitions to `--text-primary`
- Padding: `0.75rem 1rem`, max-width 1200px centered with auto margins
- A subtle bottom border: `border-bottom: 1px solid rgba(255,255,255,0.08)`

```css
/* --------------------------------------------------------------------------
   Site Navigation
   -------------------------------------------------------------------------- */

.site-nav {
  position: sticky;
  top: 0;
  z-index: 100;
  background: rgba(15, 15, 15, 0.95);
  backdrop-filter: blur(8px);
  border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 0.75rem 1rem;
  max-width: 1200px;
  margin: 0 auto;
}

.nav-logo {
  font-weight: 700;
  font-size: 1.1rem;
  color: var(--text-primary);
  text-decoration: none;
}

.nav-links {
  display: flex;
  gap: 1.5rem;
}

.nav-links a {
  color: var(--text-secondary);
  text-decoration: none;
  font-size: 0.9rem;
  transition: color 0.2s;
}

.nav-links a:hover {
  color: var(--text-primary);
}
```

**3. Create `public/og.png`:**

Generate a static 1200x630 PNG Open Graph image using an inline Node.js script. The image should have:
- Dark background (#0f0f0f) matching the site theme
- "Revolution Index" text centered, large and bold, in white (#f5f5f5)
- A subtitle line below: "Political Stability Dashboard" in the secondary text color (#a0a0a0)
- The accent color (#e85d04) used as a horizontal accent line or decorative element
- Simple but professional — no complex graphics needed

Use this approach: Create a simple HTML file, render it to canvas via a quick script, or use the built-in `canvas` approach. If `canvas` npm package is not available (it is not in package.json), create the OG image using a minimal SVG-to-PNG approach:

Write a temporary Node.js script (`scripts/generate-og.js`) that:
1. Creates an SVG string with the design described above (1200x630, dark bg, white text, accent line)
2. Writes it as `public/og.svg` first
3. Since we cannot easily convert SVG to PNG without sharp/canvas, save the SVG as `public/og.png` is NOT feasible without dependencies

**REVISED APPROACH:** Since we cannot install sharp or canvas (keeping dependencies minimal per SITE-05), create the OG image as an SVG file at `public/og.svg` and reference it as the OG image. SVG is supported by most social media crawlers (Facebook, X, LinkedIn). Update the default `ogImage` prop to `'/og.svg'` instead of `'/og.png'`.

Actually — social crawlers do NOT reliably support SVG for og:image. The simplest correct approach: Create a minimal HTML file and instruct the user it needs manual screenshot, OR commit a programmatically-generated PNG.

**FINAL APPROACH:** Use Node.js with the `child_process` module to run a simple script that writes raw PNG bytes. Actually this is too complex without libraries.

**SIMPLEST CORRECT APPROACH:** Create `public/og.png` as a placeholder by writing a script that generates it. Since no image library is available, take this two-step approach:
1. Create `public/og.svg` with the design (SVG is text, easy to generate)
2. Document that `public/og.png` needs to be created from this SVG before launch (add as a TODO comment in BaseLayout.astro near the og:image tag)
3. Set the default ogImage to `/og.svg` for development — it works in browsers and OG debugger tools
4. Add a `<!-- TODO: Convert og.svg to og.png for maximum social media compatibility before launch -->` comment

For now, create `public/og.svg` (1200x630) with:
```svg
<svg xmlns="http://www.w3.org/2000/svg" width="1200" height="630" viewBox="0 0 1200 630">
  <rect width="1200" height="630" fill="#0f0f0f"/>
  <line x1="400" y1="340" x2="800" y2="340" stroke="#e85d04" stroke-width="3"/>
  <text x="600" y="290" text-anchor="middle" font-family="system-ui, sans-serif" font-size="64" font-weight="700" fill="#f5f5f5">Revolution Index</text>
  <text x="600" y="400" text-anchor="middle" font-family="system-ui, sans-serif" font-size="28" fill="#a0a0a0">Political Stability Dashboard</text>
</svg>
```

Set the BaseLayout default ogImage to `'/og.svg'`.
  </action>
  <verify>
    <automated>cd C:/Users/matts/RevolutionIndex && npx astro build 2>&1 | tail -5 && grep -c "og:image" dist/index.html && grep -c "og:site_name" dist/index.html && grep -c "twitter:card" dist/index.html && test -f public/og.svg && echo "OG image exists" && grep -c "site-nav" dist/index.html</automated>
  </verify>
  <done>
    - BaseLayout.astro accepts description and ogImage props with defaults
    - All pages include og:type, og:url, og:site_name, og:title, og:description, og:image, og:image:width, og:image:height meta tags
    - All pages include twitter:card (summary_large_image), twitter:title, twitter:description, twitter:image meta tags
    - og:image uses absolute URL via `new URL(ogImage, Astro.site)` — not a relative path
    - og:site_name is "Revolution Index" (required for WhatsApp previews)
    - Sticky nav bar with "Revolution Index" logo link and links to Dashboard (/) and Methodology (/methodology)
    - public/og.svg exists as the OG preview image (1200x630)
    - `npm run build` passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Create methodology page, add disclaimer and about section to dashboard</name>
  <files>src/pages/methodology.astro, src/pages/index.astro, src/styles/global.css</files>
  <action>
**1. Create `src/pages/methodology.astro`:**

Create a new Astro page with four expandable `<details>` sections. All sections start collapsed (no `open` attribute). Use the `name="methodology"` attribute for exclusive accordion behavior (only one open at a time — supported in Chromium 120+ and Firefox 130+; degrades gracefully to allow-multiple-open in older browsers).

```astro
---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout
  title="Methodology — Revolution Index"
  description="How the Revolution Index score is calculated: data sources, model approach, score calculation, and limitations."
>
  <main class="methodology-page">
    <h1>Methodology</h1>
    <p class="methodology-intro">
      How the Revolution Index score is constructed. This page documents our data sources,
      modeling approach, score calculation method, and known limitations.
    </p>

    <div class="accordion">
      <details name="methodology">
        <summary>Data Sources</summary>
        <div class="accordion-body">
          <p><em>Coming soon.</em> This section will document the publicly available social, economic,
          and political data sources used to calculate the Revolution Index score. Sources will include
          economic indicators, social sentiment measures, institutional trust metrics, and political
          polarization data.</p>
        </div>
      </details>

      <details name="methodology">
        <summary>Model Approach</summary>
        <div class="accordion-body">
          <p><em>Coming soon.</em> This section will explain the modeling methodology — how raw data
          signals are transformed into normalized factor scores, and the theoretical framework behind
          the composite index. The approach draws on historical patterns of political instability
          and structural vulnerability indicators.</p>
        </div>
      </details>

      <details name="methodology">
        <summary>Score Calculation</summary>
        <div class="accordion-body">
          <p><em>Coming soon.</em> This section will detail how individual factor scores are weighted
          and combined into the composite 0–100 Revolution Index score. It will cover the weighting
          methodology, zone threshold definitions (Stable, Elevated Tension, Crisis Territory,
          Revolution Territory), and how weights may be adjusted as the model evolves.</p>
        </div>
      </details>

      <details name="methodology">
        <summary>Limitations</summary>
        <div class="accordion-body">
          <p><em>Coming soon.</em> This section will candidly address known limitations of the model
          and score, including data availability gaps, model assumptions, the inherent difficulty of
          quantifying complex political dynamics, and why this index should be understood as one
          analytical lens among many — not a definitive prediction.</p>
        </div>
      </details>
    </div>
  </main>
</BaseLayout>
```

**2. Add disclaimer section to `src/pages/index.astro`:**

Add a disclaimer `<section>` AFTER the closing `</div>` of `.dashboard-lower` and BEFORE the Gauge/TrendChart script components. The disclaimer must use neutral, analytical language per STATE.md guidance (not alarmist, not political advocacy). Add a `<!-- TODO: review disclaimer copy before launch -->` comment.

```astro
<!-- Disclaimer -->
<!-- TODO: review disclaimer copy before launch -->
<section class="disclaimer" aria-label="Score disclaimer">
  <p>
    <strong>Note:</strong> The Revolution Index is a data-driven indicator derived from
    publicly available social, economic, and political signals. It is <em>not</em> a prediction
    of future events, a call to action, or a statement of political advocacy. The score
    reflects historical patterns and structural indicators — it does not forecast specific
    outcomes. The data displayed is currently based on demonstration data and does not reflect
    real-time research.
  </p>
</section>
```

**3. Add about section to `src/pages/index.astro`:**

Add an About `<section>` AFTER the disclaimer section. Keep it concise (3-4 sentences). Link to the methodology page.

```astro
<!-- About -->
<section class="about" aria-label="About this project">
  <h2 class="section-title">About</h2>
  <p>
    The Revolution Index is an analytical project that tracks structural indicators of political
    instability in the United States. By aggregating publicly available economic, social, and
    political data into a single composite score, it aims to make complex dynamics accessible
    and understandable at a glance. This is a research and transparency tool — not a political
    statement. For details on how the score is constructed, see the
    <a href="/methodology">Methodology</a> page.
  </p>
</section>
```

**4. Add styles for methodology page, disclaimer, and about section to `src/styles/global.css`:**

Add at the END of global.css:

```css
/* --------------------------------------------------------------------------
   Methodology Page
   -------------------------------------------------------------------------- */

.methodology-page {
  max-width: 800px;
  margin: 0 auto;
  padding: 2rem 1rem 3rem;
}

.methodology-page h1 {
  font-size: 2rem;
  margin-bottom: 0.5rem;
}

.methodology-intro {
  color: var(--text-secondary);
  margin-bottom: 2rem;
  line-height: 1.7;
}

.accordion {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}

.accordion details {
  background: var(--bg-card);
  border-radius: 8px;
  overflow: hidden;
}

.accordion summary {
  padding: 1rem 1.25rem;
  font-weight: 600;
  font-size: 1.1rem;
  cursor: pointer;
  list-style: none;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.accordion summary::-webkit-details-marker {
  display: none;
}

.accordion summary::after {
  content: '+';
  font-size: 1.25rem;
  color: var(--text-secondary);
  transition: transform 0.2s;
}

.accordion details[open] summary::after {
  content: '\2212'; /* minus sign */
}

.accordion-body {
  padding: 0 1.25rem 1.25rem;
  color: var(--text-secondary);
  line-height: 1.7;
}

.accordion-body em {
  color: var(--accent);
}

/* --------------------------------------------------------------------------
   Disclaimer
   -------------------------------------------------------------------------- */

.disclaimer {
  margin-top: 2rem;
  padding: 1.25rem;
  background: var(--bg-card);
  border-radius: 8px;
  border-left: 3px solid var(--text-secondary);
}

.disclaimer p {
  margin: 0;
  font-size: 0.875rem;
  color: var(--text-secondary);
  line-height: 1.7;
}

.disclaimer strong {
  color: var(--text-primary);
}

/* --------------------------------------------------------------------------
   About Section
   -------------------------------------------------------------------------- */

.about {
  margin-top: 2rem;
}

.about p {
  color: var(--text-secondary);
  line-height: 1.7;
}

.about a {
  color: var(--accent);
  text-decoration: none;
  border-bottom: 1px solid transparent;
  transition: border-color 0.2s;
}

.about a:hover {
  border-bottom-color: var(--accent);
}
```

**IMPORTANT design notes (from project skill .claude/skills/frontend-design/SKILL.md):**
- The dark theme and CSS custom property system are already established — follow the existing aesthetic
- The accordion uses the `+`/`-` toggle indicator pattern (custom summary marker) for polish
- The "Coming soon" text uses the accent color (`--accent: #e85d04`) to draw attention
- Disclaimer uses a left border accent for visual distinction without being alarming
- All new styles use the existing CSS variable system for consistency
  </action>
  <verify>
    <automated>cd C:/Users/matts/RevolutionIndex && npx astro build 2>&1 | tail -5 && test -f dist/methodology/index.html && echo "Methodology page built" && grep -c "details" dist/methodology/index.html && grep -c "disclaimer" dist/index.html && grep -c "about" dist/index.html</automated>
  </verify>
  <done>
    - /methodology page exists with four expandable `<details>` sections: Data Sources, Model Approach, Score Calculation, Limitations
    - All sections start collapsed (no `open` attribute)
    - Sections use `name="methodology"` for exclusive accordion behavior
    - Each section body contains "Coming soon" placeholder text with context about what will be documented
    - Dashboard (index.astro) includes a disclaimer section below the dashboard-lower content
    - Disclaimer uses neutral, analytical language: "data-driven indicator," "not a prediction," "demonstration data"
    - Dashboard includes an About section with project description and link to /methodology
    - All new content styled consistently with existing dark theme using CSS custom properties
    - `npm run build` produces both dist/index.html and dist/methodology/index.html
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with no errors
2. `dist/index.html` contains og:image, og:title, og:description, og:site_name, twitter:card meta tags
3. `dist/methodology/index.html` exists and contains four `<details>` elements
4. `dist/index.html` contains the disclaimer section with "not a prediction" text
5. `dist/index.html` contains the about section with link to /methodology
6. Both pages have a site-wide nav bar with links to / and /methodology
7. `public/og.svg` exists at 1200x630 dimensions
8. OG image URL is absolute (starts with https://revolutionindex.pages.dev)
</verification>

<success_criteria>
- Methodology page at /methodology with 4 expandable sections covering data sources, model approach, score calculation, and limitations (CONT-01, CONT-02)
- Disclaimer visible on dashboard explaining what the score is and is not (CONT-03)
- About section on dashboard explaining project purpose with link to methodology (CONT-04)
- All pages include Open Graph and Twitter Card meta tags with absolute URLs (SITE-03)
- Site-wide nav bar connecting dashboard and methodology pages
- Build passes, no console errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-and-launch/03-01-SUMMARY.md`
</output>
