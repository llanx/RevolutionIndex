---
phase: 03-content-and-launch
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: []
autonomous: false
requirements: [SITE-04]

must_haves:
  truths:
    - "Site is live at a *.pages.dev URL and loads correctly in a fresh browser with no console errors"
    - "Navigating to /methodology on the live site shows the methodology page"
  artifacts: []
  key_links:
    - from: "GitHub repository (master branch)"
      to: "Cloudflare Pages deployment"
      via: "Cloudflare Pages Git integration"
      pattern: "revolutionindex.pages.dev"
---

<objective>
Deploy the Revolution Index site publicly on Cloudflare Pages.

Purpose: Make the site publicly accessible so visitors can view the dashboard and methodology page. This is the final step of the v1 milestone.

Output: Live site at revolutionindex.pages.dev (or assigned *.pages.dev URL).
</objective>

<execution_context>
@C:/Users/matts/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matts/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-and-launch/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify build and push all code to GitHub</name>
  <files></files>
  <action>
1. Run `npm run build` and confirm it completes with no errors
2. Verify the dist/ output contains:
   - `dist/index.html` with OG meta tags, disclaimer, about section, nav
   - `dist/methodology/index.html` with four details sections
   - `dist/og.svg` (copied from public/)
   - `dist/data/current.json`, `dist/data/history.json`, `dist/data/factors.json`
3. Run `git status` to check for any uncommitted changes
4. If there are uncommitted changes, stage and commit them
5. Run `git push origin master` to ensure all code is on GitHub

If `git push` fails due to authentication, this will need a checkpoint. Otherwise proceed.
  </action>
  <verify>
    <automated>cd C:/Users/matts/RevolutionIndex && npm run build 2>&1 | tail -3 && test -f dist/index.html && test -f dist/methodology/index.html && echo "Build verified" && git status --porcelain | wc -l</automated>
  </verify>
  <done>
    - `npm run build` passes with no errors
    - dist/ contains all expected files (index.html, methodology/index.html, og.svg, data/*.json)
    - All code pushed to GitHub master branch
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Connect repository to Cloudflare Pages and deploy</name>
  <files></files>
  <action>
This is a human-action checkpoint. Claude cannot access the Cloudflare dashboard.

The user must connect the GitHub repository to Cloudflare Pages:

1. Go to https://dash.cloudflare.com
2. Navigate to **Workers and Pages** > **Create** > **Pages** > **Connect to Git**
3. Authorize GitHub if not already connected
4. Select the **RevolutionIndex** repository
5. Configure build settings:
   - Framework preset: Astro
   - Build command: `npm run build`
   - Build output directory: `dist`
6. Add environment variable: `NODE_VERSION` = `20`
7. Click **Save and Deploy**
8. Wait for the build to complete (usually 1-2 minutes)

After deployment, verify:
- Visit the deployed URL (e.g., revolutionindex.pages.dev)
- Dashboard loads with score, zone label, factors, disclaimer, about section
- Nav bar links to Dashboard and Methodology
- Click Methodology link — 4 expandable sections load
- Open DevTools console — no JavaScript errors
- Test OG tags at https://www.opengraph.xyz/ — title, description, and image appear
  </action>
  <verify>
    <automated>echo "Human checkpoint — deployment verified by user"</automated>
  </verify>
  <done>
    - Site is live at a *.pages.dev URL
    - Dashboard and methodology pages load correctly
    - No console errors in browser DevTools
    - OG meta tags display correctly in share preview tools
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds locally
2. All code is pushed to GitHub
3. Site is live at a *.pages.dev URL
4. Dashboard page loads with all content (score, zone, factors, disclaimer, about)
5. Methodology page loads at /methodology with 4 expandable sections
6. No JavaScript console errors in browser DevTools
7. OG meta tags render correctly in social share preview tools
</verification>

<success_criteria>
- Site is deployed and publicly accessible on Cloudflare Pages (SITE-04)
- Both / and /methodology routes load correctly on the live URL
- No console errors in a fresh browser
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-and-launch/03-02-SUMMARY.md`
</output>
