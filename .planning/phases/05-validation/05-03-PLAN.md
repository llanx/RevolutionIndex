---
phase: 05-validation
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - models/validate.py
  - models/VALIDATION.md
autonomous: false
requirements:
  - TEST-07
must_haves:
  truths:
    - "Running `python models/validate.py` generates models/VALIDATION.md with all validation results"
    - "VALIDATION.md contains episode-by-episode results table, LOOCV table, sensitivity table, trend checklist, and overall verdict"
    - "VALIDATION.md has an explicit PASS or FAIL verdict with methodology documentation"
    - "Honest disclosure of limitations (data gaps, single time-point correlation, theoretical sensitivity bounds) is included"
  artifacts:
    - path: "models/VALIDATION.md"
      provides: "Generated validation report with pass/fail assessment"
      min_lines: 80
    - path: "models/validate.py"
      provides: "Report generation function added"
  key_links:
    - from: "models/validate.py"
      to: "models/VALIDATION.md"
      via: "write_validation_report() writes the file"
      pattern: "VALIDATION\\.md"
---

<objective>
Add the report generation function to `models/validate.py` that writes `models/VALIDATION.md` with complete validation results and pass/fail assessment. Then run the validation and verify the report through human review.

Purpose: This plan completes TEST-07 by producing the final validation report that documents whether the model produces meaningful signal. The report is the primary deliverable of Phase 5.

Output: A generated `models/VALIDATION.md` file with structured sections covering all validation analyses, and a human-verified assessment of the results.
</objective>

<execution_context>
@C:/Users/matts/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/matts/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-validation/05-CONTEXT.md
@.planning/phases/05-validation/05-RESEARCH.md
@.planning/phases/05-validation/05-01-SUMMARY.md
@.planning/phases/05-validation/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add report generation and run full validation</name>
  <files>models/validate.py, models/VALIDATION.md</files>
  <action>
Add the following to models/validate.py:

1. **`write_validation_report(validation_results: dict) -> None`**:
   Write models/VALIDATION.md with the following sections:

   ```markdown
   # Validation Report: Revolution Index

   **Generated:** {ISO timestamp}
   **Mode:** {history-only or full}
   **Overall Verdict:** {PASS or FAIL or INCOMPLETE}

   ## Summary

   {1-2 paragraph summary of findings}

   ## 1. Episode Backtesting

   ### In-Sample Anchors (Calibration Verification)

   | Episode | Expected Zone | Expected Score | Actual Score | Actual Zone | Result |
   |---------|---------------|----------------|--------------|-------------|--------|
   {table rows from episode_results where is_in_sample=True}

   ### Out-of-Sample Hold-Outs (True Test)

   | Episode | Expected Zone | Expected Range | Actual Score | Actual Zone | Result |
   |---------|---------------|----------------|--------------|-------------|--------|
   {table rows from episode_results where is_in_sample=False}

   **Zone Accuracy:** {X}% ({N} of {M} episodes in correct zone)
   **Monotonic Ordering:** {PASS/FAIL} (all crisis episodes score higher than all stability episodes)

   ## 2. Leave-One-Out Cross-Validation (LOOCV)

   {If LOOCV was run:}
   | Anchor | Target | Predicted | Deviation | Overfitting? |
   |--------|--------|-----------|-----------|--------------|
   {table rows}

   {If not run: "LOOCV requires --full mode with live pipeline data. Skipped in history-only mode."}

   ## 3. Weight Sensitivity Analysis

   | Model | Perturbation | Baseline | Shift | Fragile? |
   |-------|-------------|----------|-------|----------|
   {table rows}

   **Threshold:** Score shift > 25 points (1 zone) from a single +/-20% weight change indicates fragility.

   ## 4. Inter-Model Correlation

   {Results or "Requires --full mode" note}

   ## 5. Spurious Trend Detection

   ### Decade Summary
   | Decade | Mean Score | Min | Max | Trend |
   |--------|-----------|-----|-----|-------|

   ### Automated Checks
   - [ ] Monotonic increase across all decades: {PASS/FLAG}
   - [ ] Data boundary jumps (>10 points): {PASS/FLAG with details}
   - [ ] Score saturation (0 or 100 for 3+ consecutive points): {PASS/FLAG}

   **Note:** Spurious trend detection is an automated checklist. Final judgment on whether trends are genuine or artifactual requires human review of the historical context.

   ## 6. Bootstrap CI Width

   {CI width results or "Requires --full mode" note}

   ## Pass/Fail Criteria

   | Criterion | Result | Details |
   |-----------|--------|---------|
   | Zone accuracy >= 75% | {PASS/FAIL} | {X}% ({N}/{M}) |
   | Monotonic ordering (crisis > stability) | {PASS/FAIL} | {details} |
   | Calibration residuals <= 15 | {PASS/FAIL/N/A} | Max residual: {X} |
   | No fragile weight sensitivity | {PASS/WARN} | Max shift: {X} |
   | No spurious trends | {PASS/WARN} | {details} |

   ## Overall Verdict

   **{PASS / FAIL / INCOMPLETE}**

   {Explanation of verdict}

   ## Limitations

   {Honest disclosure of limitations:}
   1. History starts at 1979, so 1960s and Watergate episodes cannot be evaluated
   2. History.json contains demo/cached data, not live API data
   3. LOOCV has low statistical power with only 5 anchors (2 degrees of freedom per refit)
   4. Inter-model correlation from a single time point is not true temporal correlation
   5. Weight sensitivity in history-only mode shows theoretical bounds, not actual perturbation results
   6. {Any additional limitations discovered during validation}

   ## Methodology

   {Brief description of validation approach:}
   - Episode backtesting: extract scores from calibrated history at episode dates, compare to expected zone classifications
   - Zone boundaries: Stable (0-25), Elevated Tension (26-50), Crisis Territory (51-75), Revolution Territory (76-100)
   - LOOCV: hold out each calibration anchor, refit with remaining 4, measure prediction error on held-out point
   - Weight sensitivity: perturb MODEL_WEIGHTS by +/-20%, renormalize, measure score shift
   - Spurious trends: automated checks for monotonic increase, data boundary artifacts, saturation

   ---
   *Generated by models/validate.py on {date}*
   ```

2. **Update main()** to call write_validation_report(validation_results) after all analyses complete. Print a message confirming the report was written: "Validation report written to models/VALIDATION.md"

3. **Run the validation**: Execute `python models/validate.py` and verify VALIDATION.md is generated correctly.

IMPORTANT: Do NOT use em dashes in any string literals or generated markdown. Use commas, colons, or restructure instead.
IMPORTANT: The report should be honest about what was tested and what limitations exist. Do not fabricate results or hide concerning findings.
  </action>
  <verify>
    <automated>cd C:/Users/matts/RevolutionIndex && python models/validate.py && test -f models/VALIDATION.md && echo "Report generated successfully"</automated>
  </verify>
  <done>
    - models/VALIDATION.md is generated by running validate.py
    - Report contains all 6 sections: episodes, LOOCV, sensitivity, correlation, trends, CI
    - Report has overall PASS/FAIL verdict with clear criteria table
    - Limitations section is honest and comprehensive
    - Methodology section documents the approach
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human review of validation results and VALIDATION.md report</name>
  <files>models/VALIDATION.md</files>
  <action>
    Human reviews the generated validation report and console output.

    What was built:
    - Complete validation suite for the Revolution Index scoring model
    - Episode backtesting against 10 historical episodes (5 in-sample + 5 out-of-sample)
    - Leave-one-out cross-validation on calibration anchors (if --full mode available)
    - Weight sensitivity analysis
    - Inter-model correlation check
    - Spurious trend detection with automated flagging
    - Bootstrap CI width verification
    - Generated VALIDATION.md report with pass/fail assessment

    How to verify:
    1. Read `models/VALIDATION.md` and verify:
       - Episode results: Do the scores align with your historical intuition? (2008/2020 should be high, 1990s should be low)
       - Any flagged concerns in spurious trend detection
       - Overall verdict makes sense given the data
    2. Run `python models/validate.py` and check console output is readable
    3. If you have a FRED API key available: run `python models/validate.py --full` for complete validation with LOOCV and live data
    4. Key question: Given the validation results, do you trust the model enough to proceed?

    Resume signal: Type "approved" if validation results are acceptable, or describe issues to address.
  </action>
  <verify>
    <automated>test -f models/VALIDATION.md && echo "VALIDATION.md exists"</automated>
  </verify>
  <done>Human has reviewed VALIDATION.md and approved the validation results, or has provided feedback for adjustments.</done>
</task>

</tasks>

<verification>
1. `python models/validate.py` runs without errors
2. `models/VALIDATION.md` is generated with all required sections
3. Report contains honest pass/fail assessment
4. Human reviews and approves the validation results
</verification>

<success_criteria>
- VALIDATION.md contains all 6 analysis sections plus verdict, limitations, and methodology
- Report honestly documents what was tested and what limitations exist
- Console output and markdown report are consistent
- Human verifies the results are reasonable
</success_criteria>

<output>
After completion, create `.planning/phases/05-validation/05-03-SUMMARY.md`
</output>
