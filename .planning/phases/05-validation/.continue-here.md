---
phase: 05-validation
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-03-05T02:26:00.406Z
---

<current_state>
Phase 05 execution is mid-wave-3. Plans 05-01 and 05-02 are complete (committed). Plan 05-03 reached a human-verify checkpoint (task 1/2 done, task 2 awaiting approval). The user chose to run /review-model before approving, which found 4 theory-to-code issues. Fix 1 of 4 (bootstrap perturbation mismatch) has been applied. The remaining 3 fixes and the checkpoint approval are pending.
</current_state>

<completed_work>

- Wave 1 (05-01): Core validation script created. Commits: 0a6ed6e, eae228e, 633cc65
- Wave 2 (05-02): Diagnostic analyses added. Commits: a259cd8, f35cda3
- Wave 3 (05-03): Task 1 done (report generation, commit b9036e1). Task 2 (human verify) paused for model review.
- /review-model completed: 4 issues found (1 SERIOUS, 3 MODERATE)
- Fix applied: Check 6 bootstrap perturbation mismatch in calibrate.py (split _perturb_within_domains into _generate_domain_swaps + _apply_domain_swaps so both normalized and raw DataFrames get identical swaps)
</completed_work>

<remaining_work>

- Apply 3 remaining /review-model fixes:
  1. Check 2 (MODERATE): PLI computes deviation/velocity on CDF-normalized data. Fix: pass raw_df to PLI, compute reference and deviation from raw values.
  2. Check 4 (MODERATE): PLI reference uses 5-year mean instead of trough. Fix: change series.iloc[-REFERENCE_WINDOW:].mean() to .min() in model_pli.py:133.
  3. Check 1 (MODERATE): Variable #26 name says "16-19 + 20-24" but only fetches LNS14000012 (16-19). Fix: update name to match actual data or change to CONSTRUCTED.
- Commit all fixes
- Return to phase 05-03 checkpoint: approve validation results (type "approved")
- Complete 05-03 (continuation agent needed for task 2)
- Run phase verification + quality profiler
- Update roadmap if verification passes
</remaining_work>

<decisions_made>

- Ran /review-model before approving validation checkpoint to identify theory-to-code issues
- Bootstrap fix uses split generate/apply pattern: _generate_domain_swaps creates the mapping once, _apply_domain_swaps applies it to each DataFrame
- PLI reference point: mean vs trough is a theory question. Trough (min) better matches Kahneman-Tversky adaptation-level concept where people anchor to best recent experience
- Variable #26: simplest fix is updating the name to match actual implementation (single FRED series)
</decisions_made>

<blockers>
- None. All remaining work is straightforward code changes.
</blockers>

<context>
The execute-phase orchestrator was running phase 05. Waves 1-2 completed successfully. Wave 3 hit a checkpoint. Instead of approving, user ran /review-model --fix to find theory bugs first. The review found the bootstrap perturbation mismatch was the most impactful bug (SERIOUS). The PLI issues (Checks 2+4) may improve validation scores by fixing how losses are measured. After applying all fixes, re-run validation (python models/validate.py) to see if scores improve, then approve the checkpoint.

The execute-phase workflow needs to resume from: spawning a continuation agent for 05-03 task 2 with user_response="approved", then proceeding to verify_phase_goal step.
</context>

<next_action>
1. Apply remaining 3 model review fixes (PLI raw_df, PLI reference trough, var #26 name)
2. Commit all fixes together
3. Re-run `python models/validate.py` to check if scores improved
4. Resume execute-phase: approve 05-03 checkpoint, complete wave 3, run verification
</next_action>
